# endpoint-deployer Helm Chart

A **simple Helm chart** designed to deploy an ingress for services outside of Kubernetes.


## ⚙️ Installation

### Add the Helm Repository
```bash
helm repo add endpoint-deployer https://github.com/Sony2k20/endpoint-deployer-helm
helm repo update