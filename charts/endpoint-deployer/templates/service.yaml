apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-chart.fullname" . }}
  labels:
    {{- include "helm-chart.labels" . | nindent 4 }}
spec:
spec:
  ports:
  - port: 80
    targetPort: {{ .Values.service.port }}
  clusterIP: None